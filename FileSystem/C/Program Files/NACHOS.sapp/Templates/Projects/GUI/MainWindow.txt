using System;
using Microsoft.Xna.Framework;
using TheGame.Core.UI;
using TheGame.Core.UI.Controls;

namespace {namespace};

public class MainWindow : Window {
    public MainWindow() : base(Vector2.Zero, new Vector2({windowWidth}, {windowHeight})) {
        Title = "{windowTitle}";
        CanResize = {windowResizable};
    }

    protected override void OnLoad() {
<sample>
        AddChild(new Label(new Vector2(20, 20), "Welcome to {projectName}!") {
            FontSize = 24
        });
</sample>
<noSample>
        // Add your initialization code here
</noSample>
    }
}
